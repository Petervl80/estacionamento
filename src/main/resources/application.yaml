spring:
  application:
    name: estacionamento
  datasource:
    url: jdbc:mysql://localhost:3309/estacionamento_db
    username: admin
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  main:
    web-application-type: none